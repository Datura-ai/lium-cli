class Defaults:
    class config:
        path = "~/.celium/config.yaml"
        base_path = "~/.celium"
        dictionary = {
            "docker_username": None,
            "docker_password": None,
            "api_key": None,
            "server_url": "https://celiumcompute.ai",
        }

defaults = Defaults